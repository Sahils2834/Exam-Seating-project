import React from "react";

export default function AdminSettings(){
  return (
    <div>
      <h1 className="page-title">Admin Settings</h1>
      <div className="card">
        <p>Site / SMTP / Security settings can be edited here.</p>
        <p>Right now placeholder — wiring to backend settings API can be added.</p>
      </div>
    </div>
  );
}
